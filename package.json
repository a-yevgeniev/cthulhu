{
  "name": "coc7-dice-roller",
  "private": true,
  "version": "0.1.0",
  "description": "Call of Cthulhu 7e dice roller — rules engine + web app.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspace=coc7-engine",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev": "npm run dev --workspace=coc7-web",
    "dev:server": "npm run dev --workspace=coc7-server"
  },
  "license": "MIT"
}
